name: "Mirroring"
description: "Mirror a repository"
inputs:
  ssh_private_key:
    description: "SSH private key for ssh connection to the target repository"
    required: false
  target_repo_url:
    description: "Target url"
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
